To install simply copy the line below

	git clone git://github.com/lupo342/vim-configs.git ~/.vim



After installation create the following symbolic links
	
	ln -s ~/.vim/vimrc ~/.vimrc
	ln -s ~/.vim/gvimrc ~/.gvimrc



